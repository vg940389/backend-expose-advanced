apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "backend-expose-advanced.fullname" . }}-test-connection"
  labels:
    {{- include "backend-expose-advanced.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "backend-expose-advanced.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
